/**
 * Source unique de la version. Importee par l'interface ET par le Worker :
 * c'est la comparaison des deux qui permet de detecter un onglet perime.
 *
 * x.y.z — z : correction, y : nouvelle fonction, x : refonte.
 * A modifier dans le meme commit que le changement, avec une ligne dans CHANGELOG.md.
 */
export const VERSION = '0.12.0'
